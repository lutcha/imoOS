/**
 * Obra Section Layout
 * Pode adicionar metadata específica para a secção de obra
 */
export const metadata = {
  title: "Obra — ImoOS",
};

export default function ObraLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

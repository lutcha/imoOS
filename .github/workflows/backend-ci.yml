# Superseded by ci.yml — kept only for legacy branch protection rules.
# All backend CI runs in ci.yml (lint, test, isolation-tests, security-scan).
name: Backend CI (legacy alias)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  notice:
    name: "See ci.yml for backend tests"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Backend CI has moved to ci.yml. This job is a no-op alias."
